export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <div role="status" className="w-full h-screen">
            <span className="text-4xl top-1/2"><center>Loading...</center></span>
        </div>
    );
  }